import { DropZoneDirectiveDirective } from './drop-zone-directive.directive';

describe('DropZoneDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DropZoneDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
